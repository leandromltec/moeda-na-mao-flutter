//import 'package:moedanamao/app/controllers/controllers_app/shared.dart';

class BitCoin {
  /*String valor;
  String alta;
  String baixa;

  BitCoin(this.valor);

  BitCoin.fromJson(Map<String, dynamic> json) {
    //Valor de Compra
    valor = convertReais(json['buy']);

    //Valor de alta
    alta = convertReais(json['high']);

    //Valor de baixa
    baixa = convertReais(json['low']);
  }*/
}
